name: My First CI

on:
  push:
    branches: [ main ]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Say hello
        run: echo "CI работает! Мой код в безопасности."
